#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv)
{

    int var_int=0;
    char var_char=256;
    float var_float;
    double var_double;

    //ld = long decimal, porque queremos a representacao numerica em long decimal, nao binario ou octal.
    //sizeof isn't exactly considered a function

        printf("var_int: %ld\n",sizeof(var_int));
        printf("var_char: %ld\n",sizeof(var_char));
        printf("var_float: %ld\n",sizeof(var_float));
        printf("var_double %ld\n",sizeof(var_double));

    //sizeof returns the size in bytes, allocated in memory, for each variable;

    //testing of printf:
    //you can only put variables, number operations, number operations with variables inside a value corresponding to their placeholder














































































    
        printf("30+41= %d", 30+70);

    return 0;

}